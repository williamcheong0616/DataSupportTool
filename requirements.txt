# Web Framework (API only - frontend is React)
fastapi==0.128.2
uvicorn==0.40.0
python-multipart==0.0.22
httpx==0.28.1

# Database
sqlalchemy==2.0.46
alembic==1.18.3
psycopg2-binary==2.9.11

# Data Processing
pandas==2.3.3
numpy==2.2.6
pydantic==2.12.5

# ML & Evaluation
scikit-learn

# Task Queue (Celery + Redis)
celery==5.6.2
redis==7.1.0
flower==2.0.1

# HTTP Client (Ollama / external API calls)
requests==2.32.5

# YouTube Audio Download
yt-dlp==2026.3.3

# Utilities
python-dotenv==1.2.1
aiofiles==24.1.0

# Logging
python-json-logger==4.0.0

# ASR — local inference (lazy-loaded, only needed if running Whisper/VAD locally)
# Install PyTorch separately first: https://pytorch.org/get-started/locally/
faster-whisper==1.2.1
silero-vad==6.2.1
pydub==0.25.1
nvidia-cublas-cu12  # provides libcublas.so.12 that CTranslate2 requires regardless of system CUDA version

# ASR — Qwen3 (optional, only if using Qwen3-ASR model)
qwen-asr
